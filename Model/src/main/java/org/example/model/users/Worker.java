package org.example.model.users;

public class Worker extends Personnel {
    public Worker(String nickName, String password, int ID, int permLevel, String URL) {
        super(nickName, password, ID, permLevel, URL);
    }
}
